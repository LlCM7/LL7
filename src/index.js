// index.js
document.addEventListener('DOMContentLoaded', function() {
    // 创建一个用于显示文本的元素
    var textElement = document.createElement('p');
    // 设置元素的文本内容
    textElement.textContent = '名字：李乐';
    // 将元素添加到 body 元素中
    document.body.appendChild(textElement);
  });